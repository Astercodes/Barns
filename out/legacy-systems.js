const prune=document.querySelector('#pruneGrid');if(prune){const items=['Obligation','Friendship','Habit','Project','Subscription','Belief','Commitment','Distraction','Role','Possession'];prune.innerHTML=items.map(x=>`<label><input type="checkbox">${x}</label>`).join('');pruneForm.onsubmit=e=>{e.preventDefault();const n=prune.querySelectorAll('input:checked').length,r=pruneForm.querySelector('.standalone-result');r.hidden=false;r.innerHTML=`<strong>${n} area${n===1?'':'s'} named for pruning.</strong> Release is now connected to the space it will create.`}}
const promise=document.querySelector('#promiseForm');if(promise)promise.onsubmit=e=>{e.preventDefault();const type=promise.querySelector('input[name=promiseType]:checked').value;promiseResult.hidden=false;promiseResult.innerHTML=`<p class="eyebrow">${type.toUpperCase()}</p><h2>${promiseText.value||'Untitled entry'}</h2><p>Status: <b>${promiseStatus.value}</b></p>`}
const eb=document.querySelector('#ebenezerForm');if(eb)eb.onsubmit=e=>{e.preventDefault();ebenezerResult.hidden=false;ebenezerResult.innerHTML='<div class="stone-mark">E</div><div><p class="eyebrow">EBENEZER RECORDED</p><h2>Hitherto hath the LORD helped us.</h2><p>This memory has been named so future seasons can draw strength from remembered faithfulness.</p></div>'}

